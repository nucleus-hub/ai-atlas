#  Tools

Small, reusable dev utilities that support the rest of the repo (not learning material).

| Tool | Purpose |
|------|---------|
| `check_keys.py` | Loads `.env` and verifies required API keys (e.g. `GOOGLE_API_KEY`, `HF_TOKEN`) are set. |

```bash
python tools/check_keys.py
```
