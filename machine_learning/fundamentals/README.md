# ML Fundamentals

Core concepts every ML practitioner needs: the ML workflow, bias–variance tradeoff, train/validation/test splits, cross-validation, evaluation metrics, over/underfitting, regularization, feature scaling.

*Add notes and small illustrative scripts here.*
